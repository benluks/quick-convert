# configs/pipeline/training.yaml

_target_: quick_convert.pipelines.training.pipeline.TrainingPipeline
trainer: ${trainer}
train_dataset: ${train_dataset}
val_dataset: ${val_dataset}
test_dataset: null
output_dir: ${out_root}/${hydra:runtime.choices.trainer}/${hydra:runtime.choices.dataset@train_dataset}${out_dir_suffix}

batch_size: 8
val_batch_size: ${pipeline.batch_size}

dataloader:
  num_workers: 4
  pin_memory: true
  drop_last: false

train_kwargs:
  # in the future, have the lightning_trainer class interpret more implementation-agnostic
  # param names and translate them into L.Trainer kwargs
  max_epochs: 100
  accelerator: auto
  devices: auto
  precision: 32
  log_every_n_steps: 10
  enable_checkpointing: true
  enable_progress_bar: true
  enable_model_summary: true

# test_dataset: null
# test_dataloader_kwargs: {}
# run_test: false
