# configs/anonymizer/conditional_rvq.yaml (future)

defaults:
  - /speaker_encoder: pyannote_wespeaker
  - /content_encoder: whisper
  - /tokenizer: rvq
  - _self_

_target_: quick_convert.systems.anonymization.ConditionalRVQSystem

speaker_encoder: ${speaker_encoder}
content_encoder: ${content_encoder}
tokenizer: ${tokenizer}
