# @package _global_

out_root: outputs
out_dir_suffix: ""
device: cuda
use_flash_attention: false
# root for all datasets
data_root: /cfs/collections
# gpu: null

hydra:
  run:
    dir: ${pipeline.out_dir}
