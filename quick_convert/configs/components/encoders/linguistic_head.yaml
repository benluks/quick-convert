_target_: quick_convert.components.encoders.LinguisticCTCHead
input_dim: ???
hidden_dim: 256
decoder:
  _target_: torchaudio.models.Conformer
  input_dim: ${..hidden_dim}
  num_heads: ${floor:${.input_dim}, 64}
  ffn_dim: ${mul:${.input_dim}, 4}
  num_layers: 2
  depthwise_conv_kernel_size: 31
  dropout: 0.1
output_dim: ???
loss:
  _target_: quick_convert.components.losses.CTCLoss
