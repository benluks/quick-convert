_target_: quick_convert.components.speaker.speaker_generators.cfm_speaker_generator.CFMSpeakerGenerator

embedding_dim: 256

cfm:
  defaults:
    - components/flow_matching/estimators@estimator: residual_mlp
    - _self_
  _target_: quick_convert.components.decoders.flow_matching.base.BASECFM
  solver: euler
  estimator: ${components.flow_matching.estimators}
