# Donors

`donors/` is a directory for code that's copied verbatim, (i.e. not integrated into the codebases ultimate structure) in order to streamline experiments using external code structure.

I've so far tried to maintain the structure as closely as possible to the original repo. That may change down the line.